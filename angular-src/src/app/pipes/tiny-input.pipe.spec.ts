import { TinyInputPipe } from './tiny-input.pipe';

describe('TinyInputPipe', () => {
  it('create an instance', () => {
    const pipe = new TinyInputPipe();
    expect(pipe).toBeTruthy();
  });
});
