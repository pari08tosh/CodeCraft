module.exports = {
  // Set server mode as true when in production. When running by ng serve, set it to false.
  productionMode: false,

  // set port for server
  port: 3000,

  // set port for judge
  judgePort: 8080,
};
